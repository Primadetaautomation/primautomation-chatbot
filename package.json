{
  "name": "primautomation-chatbot",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild public/primautomation-widget.js --bundle --format=iife --outfile=public/primautomation-widget-bundle.js --platform=browser",
    "start": "npm run build && node server.js",
    "dev": "npm run build && node --watch server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.78.0",
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^16.4.0",
    "esbuild": "^0.27.3",
    "express": "^4.21.0",
    "helmet": "^8.1.0"
  }
}
